BEGIN TRY

BEGIN TRAN;

ALTER TABLE [dbo].[User] ADD
    [country] NVARCHAR(100),
    [passwordHash] NVARCHAR(255);

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW;

END CATCH